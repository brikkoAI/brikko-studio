apiVersion: v1
kind: Service
metadata:
  name: brikko-studio
  labels:
    app: brikko-studio
spec:
  type: ClusterIP
  selector:
    app: brikko-studio
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
