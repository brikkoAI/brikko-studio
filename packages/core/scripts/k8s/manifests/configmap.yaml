apiVersion: v1
kind: ConfigMap
metadata:
  name: brikko-studio-config
  labels:
    app: brikko-studio
data:
  brikko-studio.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.brikko-studio/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Brikko Studio Assistant",
            "workspace": "~/.brikko-studio/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Brikko Studio Assistant

    You are a helpful AI assistant running in Kubernetes.
