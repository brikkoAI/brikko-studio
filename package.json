{
  "name": "brikko-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Brikko Studio — российский AI-агент с reversible PII anonymization. M0 foundations.",
  "license": "MIT",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "oxlint --ignore-path .oxlintignore packages/",
    "typecheck": "tsc --build",
    "format": "prettier --write \"packages/*/src/**/*.{ts,tsx,json}\""
  },
  "devDependencies": {
    "oxlint": "^0.13.0",
    "prettier": "3.2.5",
    "typescript": "5.4.5",
    "vitest": "^4.1.5"
  },
  "packageManager": "pnpm@10.33.2"
}
